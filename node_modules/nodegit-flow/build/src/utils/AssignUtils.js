'use strict';

var noOpProperties = ['arguments', 'caller', 'callee'];

module.exports = function (to, from) {
  Object.getOwnPropertyNames(from).forEach(function (key) {
    if (!~noOpProperties.indexOf(key) && typeof from[key] === 'function') {
      to[key] = from[key];
    }
  });
};