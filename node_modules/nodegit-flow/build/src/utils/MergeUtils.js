'use strict';

var MergeUtils = {
  getMergeMessage: function getMergeMessage(toBranch, fromBranch) {
    var mergeDecorator = void 0;
    if (fromBranch.isTag()) {
      mergeDecorator = 'tag';
    } else if (fromBranch.isRemote()) {
      mergeDecorator = 'remote-tracking branch';
    } else {
      mergeDecorator = 'branch';
    }

    var message = 'Merge ' + mergeDecorator + ' \'' + fromBranch.shorthand() + '\'';

    // https://github.com/git/git/blob/master/builtin/fmt-merge-msg.c#L456-L459
    return toBranch.shorthand() !== 'master' ? message + ' into ' + toBranch.shorthand() : message;
  }
};

module.exports = MergeUtils;